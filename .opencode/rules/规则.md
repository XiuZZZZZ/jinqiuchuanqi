
# 约束
- EasyClick 运行时会**自动加载** `src/js/` 下的所有 `.js` 文件，全局函数可直接调用，不需要require("图色工具.js");
- 在能使用let的地方使用let，而不是var
- setInterval 在 easyclick 中可用

# api

## time 毫秒级当前时间戳
```js
time()  return {long} 毫秒级别的long时间
```


