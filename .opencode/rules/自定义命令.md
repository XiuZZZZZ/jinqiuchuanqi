# 自定义命令

在 TUI 输入 `/命令名` 执行。命令名+描述如下：

| 命令 | 描述 |
|------|------|
| `/build` | 编译 IEC：执行 `ec-android-cli build -m 金秋传奇` |
| `/run` | 运行脚本：执行 `ec-android-cli run -m 金秋传奇` |
| `/preview` | 预览 UI：执行 `ec-android-cli preview -m 金秋传奇` |

CLI 实际路径：`./ec_work_config/android/bin/ec-android-cli`
